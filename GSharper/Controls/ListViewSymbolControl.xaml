<UserControl 
    x:Class="GSharper.Controls.ListViewSymbolControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:GSharper.Controls"
    xmlns:ui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
    xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
    xmlns:controlss="clr-namespace:GSharper.Controls"
    mc:Ignorable="d"
    Background="#323232"
    d:DesignHeight="450" 
    d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBox
            Grid.Row="0"
            Padding="3" 
            Margin="3" 
            TextChanged="OnChangedText" 
            PreviewKeyDown="OnKeyDown"
            Name="_textBoxSearch"
            LostFocus="OnLostFocusSearch"
            Background="#323232"
            Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
            BorderBrush="#6A6287" 
            BorderThickness="1" 
        ></TextBox>

        <ProgressBar
            Grid.Row="1"
            Height="2"
            IsIndeterminate="True"
            Name="_progressBarFilter" 
            Visibility="Collapsed" />
        <TextBlock
            Grid.Row="1"
            TextWrapping="Wrap" 
            TextAlignment="Left" 
            Name="_textBlockInfo"
            Visibility="Collapsed"
            Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}" />

        <ListView
            Grid.Column="0"
            Grid.Row="2"
            Name="_listViewSymbols"
            Background="#323232"
            BorderThickness="0"
            Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            >
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnMouseSelected" />

                    <Style.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent" />
                    </Style.Resources>

                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="BorderThickness" Value="0" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <controlss:ListItemSymbolControl SymbolInstance="{Binding .}">

                        </controlss:ListItemSymbolControl>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
